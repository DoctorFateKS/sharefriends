<div class="new-profile-container">
  <h1>Crée ton Profil</h1>
  <p>Dis-nous qui tu es pour que nous puissions te proposer les meilleurs événements !</p>

  <%= form_with model: @profile, local: true do |form| %>

    <div class="form-group">
      <%= form.label :first_name, "Prénom" %>
      <%= form.text_field :first_name, class: "form-control", placeholder: "Ton prénom" %>
    </div>

    <div class="form-group">
      <%= form.label :last_name, "Nom" %>
      <%= form.text_field :last_name, class: "form-control", placeholder: "Ton nom" %>
    </div>

    <fieldset class="form-group">
      <legend>Quel est ton mood du moment ? 😃</legend>
      <div class="mood-options">
        <% ["Fêtard", "Créatif", "Explorateur", "Zen, posé"].each do |mood| %>
          <label class="mood-radio">
            <%= form.radio_button :mood, mood, id: "mood_#{mood.parameterize}" %>
            <span><%= mood %></span>
          </label>
        <% end %>
      </div>
    </fieldset>

  <fieldset class="form-group">
    <legend>Tes centres d'intérêt (min 1, max 4)</legend>
    <div class="hobbie-options">
      <% ["Geek, créatif", "Sport, découverte", "Food, partage", "Chill, cosy"].each do |hobby| %>
        <label class="hobbie-checkbox">
          <%= form.check_box :hobbies, { multiple: true }, hobby %>
          <%= hobby %>
        </label>
      <% end %>
    </div>
  </fieldset>

  <%= form.submit "Enregistrer mon Profil", class: "btn btn-success btn-block" %>
  <% end %>

  <%= link_to "⬅ Retour", authenticated_root_path, class: "btn btn-secondary" %>
</div>
