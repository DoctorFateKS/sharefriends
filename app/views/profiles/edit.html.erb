<div class="container-show">
  <h1>🛠 Modifier mon Profil</h1>

  <%= form_with model: @profile, local: true do |form| %>

  <%= form.file_field :photo%>

    <div class="form-group">
      <%= form.label :first_name, "Prénom" %>
      <%= form.text_field :first_name, class: "form-control" %>
    </div>

    <div class="form-group">
      <%= form.label :last_name, "Nom" %>
      <%= form.text_field :last_name, class: "form-control" %>
    </div>

    <div class="form-group">
      <%= form.label :mood, "Change ton mood 😃" %>
      <div class="mood-options">
        <% ["Fêtard", "Créatif", "Explorateur", "Zen, posé"].each do |mood| %>
          <label class="mood-radio">
            <%= form.radio_button :mood, mood, checked: (@profile.mood == mood) %> <%= mood %>
          </label>
        <% end %>
      </div>
    </div>

    <div class="form-group">
      <%= form.label :hobbie, "Modifie tes centres d'intérêt 🎭 (min 1, max 4)" %>
      <div class="hobbie-options">
        <% ["Geek, créatif", "Sport, découverte", "Food, partage", "Chill, cosy"].each do |hobbie| %>
          <label class="hobbie-checkbox">
            <%= form.check_box :hobbie, { multiple: true }, hobbie, @profile.hobbie&.include?(hobbie) ? hobbie : nil %> <%= hobbie %>
          </label>
        <% end %>
      </div>
    </div>

    <%= form.submit "💾 Enregistrer les modifications", class: "btn btn-primary btn-block" %>
  <% end %>

  <%= link_to "⬅ Retour", dashboard_path, class: "btn btn-secondary" %>
</div>
