<div class="body-dashboard background">
  <div class="container-dashboard">
    <div class="dashboard-text">

      <%= link_to "Créer un Événement", new_event_path, class: "btn button-dashboard" if user_signed_in? %>

<div class="container-cards-dashboard">
      <h1>Mes Événements Créés</h1>
  <% @created_events.each do |event| %>
    <%= link_to event_path(event), class: "event-card-link-dashboard" do %>
      <div class="event-card-dashboard">
        <div class="logo-container">
          <%= cl_image_tag event.photo.key, height: 300, width: 400, crop: :fill %>
          <div class="event-badges mood-logo" title="<%= event.user.mood %>">
            <% case event.user.mood %>
            <% when "Fêtard" %>
              <i class="fa-solid fa-fire"></i>
            <% when "Créatif" %>
              <i class="fa-solid fa-palette"></i>
            <% when "Explorateur" %>
              <i class="fa-regular fa-compass"></i>
            <% when "Zen" %>
              <i class="fa-solid fa-leaf"></i>
            <% else %>
              <%= event.user.mood %>
            <% end %>
          </div>
        </div>
        <div class="card-body">
          <h5 class="card-title"><%= event.title %></h5>
        </div>
      </div>
    <% end %>

    <div class="text-center mt-2">
      <%= link_to "🗑 Supprimer", event_path(event),
                  data: { turbo_method: :delete, turbo_confirm: "Supprimer cet événement ?" },
                  class: "btn btn-danger" %>
    </div>
  <% end %>
</div>

<div class="container-cards-dashboard">
  <h1>Mes Participations - En attente</h1>

  <% if @pending_events.any? %>
    <div class="event-card-list">
      <% @pending_events.each do |event| %>
        <%= link_to event_path(event), class: "event-card-link-dashboard" do %>
          <div class="event-card-dashboard">
            <div class="logo-container">
              <%= cl_image_tag event.photo.key, height: 300, width: 400, crop: :fill %>
              <div class="event-badges mood-logo" title="<%= event.user.mood %>">
                <% case event.user.mood %>
                <% when "Fêtard" %>
                  <i class="fa-solid fa-fire"></i>
                <% when "Créatif" %>
                  <i class="fa-solid fa-palette"></i>
                <% when "Explorateur" %>
                  <i class="fa-regular fa-compass"></i>
                <% when "Zen" %>
                  <i class="fa-solid fa-leaf"></i>
                <% else %>
                  <%= event.user.mood %>
                <% end %>
              </div>
            </div>
            <div class="card-body">
              <h5 class="card-title"><%= event.title %></h5>
              <p class="card-text text-muted">En attente de confirmation</p>
            </div>
          </div>
        <% end %>

        <div class="text-center mt-2">
          <%= link_to "Annuler", event_participation_path(event, event.participations.find_by(user: current_user)),
                      method: :delete,
                      data: { confirm: "Annuler ta participation ?" },
                      class: "btn btn-warning" %>
        </div>
      <% end %>
    </div>
  <% else %>
    <p>Aucune participation en attente.</p>
  <% end %>
</div>


<div class="container-cards-dashboard">
  <h1>Mes événements à venir</h1>
    <% if @accepted_events.any? %>
      <div class="event-card-list">
        <% @accepted_events.each do |event| %>
          <%= link_to event_path(event), class: "event-card-link-dashboard" do %>
            <div class="event-card-dashboard">
              <div class="logo-container">
                <%= cl_image_tag event.photo.key, height: 300, width: 400, crop: :fill %>
                <div class="event-badges mood-logo" title="<%= event.user.mood %>">
                  <% case event.user.mood %>
                  <% when "Fêtard" %>
                    <i class="fa-solid fa-fire"></i>
                  <% when "Créatif" %>
                    <i class="fa-solid fa-palette"></i>
                  <% when "Explorateur" %>
                    <i class="fa-regular fa-compass"></i>
                  <% when "Zen" %>
                    <i class="fa-solid fa-leaf"></i>
                  <% else %>
                    <%= event.user.mood %>
                  <% end %>
                </div>
              </div>
              <div class="card-body">
                <h5 class="card-title"><%= event.title %></h5>
                <p class="card-text text-success">Participation confirmée</p>
              </div>
            </div>
          <% end %>

          <div class="text-center mt-2">
            <%= link_to "Annuler", event_participation_path(event, event.participations.find_by(user: current_user)),method: :delete, data: { confirm: "Es-tu sûr de vouloir annuler ta participation ?" } %>
          </div>
        <% end %>
      </div>
    <% else %>
      <p>Aucun événement à venir.</p>
    <% end %>
</div>

<div class="container-tchat">
  <h2>Messages</h2>
    <% if @chatrooms.any? %>
      <div class="event-card-list-msg">
        <% @chatrooms.each do |chatroom| %>
          <%= link_to event_chatroom_path(chatroom.event, chatroom), class: "event-card-link-msg" do %>
            <div class="event-card-msg">
              <div class="logo-container">
                <%= cl_image_tag chatroom.event.photo.key, height: 300, width: 400, crop: :fill %>
                <div class="event-badges mood-logo" title="<%= chatroom.event.user.mood %>">
                  <% case chatroom.event.user.mood %>
                  <% when "Fêtard" %>
                    <i class="fa-solid fa-fire"></i>
                  <% when "Créatif" %>
                    <i class="fa-solid fa-palette"></i>
                  <% when "Explorateur" %>
                    <i class="fa-regular fa-compass"></i>
                  <% when "Zen" %>
                    <i class="fa-solid fa-leaf"></i>
                  <% else %>
                    <%= chatroom.event.user.mood %>
                  <% end %>
                </div>
              </div>
              <div class="card-body">
                <h5 class="card-title"><%= chatroom.event.title %></h5>
                <p class="card-text">Accéder à la discussion</p>
              </div>
            </div>
          <% end %>
        <% end %>
      </div>
    <% else %>
      <p>Aucune discussion en cours. Rejoins un événement pour discuter avec les autres participants !</p>
    <% end %>
</div>
