<h1>Bienvenue <%= current_user.first_name %> ! 🎉</h1>
<p>Retrouve ici tes événements confirmés et accède rapidement aux discussions !</p>

<h2>📅 Mes Événements Confirmés</h2>

<% if @participations.any? %>
  <div class="events-grid">
    <% @participations.each do |event| %>
      <div class="event-card">
        <h3><%= event.title %></h3>
        <p>📍 <%= event.address %></p>
        <p>🕒 <%= event.date.strftime("%d/%m/%Y %H:%M") %></p>
        <p>👥 Participants max : <%= event.max_participants %></p>

        <div class="event-actions">
          <%= link_to "🔍 Voir l'événement", event_path(event), class: "btn btn-info" %>
          <% chatroom = event.chatroom %>
          <% if chatroom %>
            <%= link_to "💬 Rejoindre le chat", event_chatroom_path(event, chatroom), class: "btn btn-primary" %>
          <% end %>
        </div>
      </div>
    <% end %>
  </div>
<% else %>
  <p>Tu n'as pas encore d'événements confirmés. Inscris-toi à un événement et commence l'aventure !</p>
  <%= link_to "Explorer les événements", explore_path, class: "btn btn-success" %>
<% end %>

<h2>💬 Mes Chatrooms</h2>

<% if @chatrooms.any? %>
  <ul class="chatroom-list">
    <% @chatrooms.each do |chatroom| %>
      <li>
        <%= link_to "💬 Discussion : #{chatroom.event.title}", event_chatroom_path(chatroom.event, chatroom), class: "chatroom-link" %>
      </li>
    <% end %>
  </ul>
<% else %>
  <p>Aucune discussion en cours. Rejoins un événement pour discuter avec les autres participants !</p>
<% end %>

